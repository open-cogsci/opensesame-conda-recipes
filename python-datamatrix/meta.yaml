package:
  name: python-datamatrix
  version: "0.3.1"

source:
  fn: python-datamatrix-0.3.1.tar.gz
  url: https://pypi.python.org/packages/3e/e2/3314cb0e6ae0d8a0f3b6a81761ca731afe987bc5fed320a962f9e9fccd96/python-datamatrix-0.3.1.tar.gz
  md5: ba3d05e5c381991db3e972441b6a1ba2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-datamatrix = python-datamatrix:main
    #
    # Would create an entry point called python-datamatrix that calls python-datamatrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datamatrix
    - datamatrix._datamatrix
    - datamatrix.colors
    - datamatrix.io
    - datamatrix.rbridge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smathot/python-datamatrix
  license: GNU General Public License v3 (GPLv3)
  summary: 'An intuitive, Pythonic way to work with tabular data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
