{% set name = "pyqode.python" %}
{% set version = "2.12.2a5" %}
{% set sha256 = "fbdd56c0596a8725d1cfa97513a4d21e7fc13d05469863d9eba58d0e86829e83" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://files.cogsci.nl/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  preserve_egg_dir: True
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv "

requirements:
  host:
    - docutils
    - jedi
    - pycodestyle
    - pip
    - pyflakes
    - pyqode.core
    - pyqode.qt
    - python
    - setuptools
  run:
    - docutils
    - jedi
    - pycodestyle
    - pyflakes
    - pyqode.core
    - pyqode.qt
    - python
    - setuptools

test:
  imports:
    - pyqode
    - pyqode.python
    - pyqode.python._forms
    - pyqode.python.backend
    - pyqode.python.managers
    - pyqode.python.modes
    - pyqode.python.panels
    - pyqode.python.widgets
  requires:
    - pyqt

about:
  home: https://github.com/pyQode/pyqode.python
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Adds python support to pyqode.core
  doc_url: https://github.com/pyQode/pyQode/wiki
  dev_url: https://github.com/pyQode/pyQode

extra:
  recipe-maintainers:
    - ericpre
