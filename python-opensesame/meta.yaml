package:
  name: python-opensesame
  version: "3.1.0.a24"

source:
  fn: python-opensesame-3.1.0a24.tar.gz
  url: https://pypi.python.org/packages/source/p/python-opensesame/python-opensesame-3.1.0a24.tar.gz
  md5: 6cd07070147aa1ba3a2016f951b71d66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-datamatrix = python-datamatrix:main
    #
    # Would create an entry point called python-datamatrix that calls python-datamatrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - webcolors
    - python-qprogedit
    - python-qdatamatrix
    - python-pseudorandom

  run:
    - python
    - pyqt
    - python-qprogedit
    - python-qdatamatrix
    - python-pseudorandom
    - pyyaml
    - webcolors
    - pyserial
    - markdown

 # test:
    # Python imports
    # imports:
   

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smathot/OpenSesame
  license: GNU General Public License v3 (GPLv3)
  summary: 'Graphical experiment builder for the social sciences'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
